# Merge Project Bundle

This folder contains the **improved HuggingClaw merge project artifacts** produced in this workspace.

## Included
- `HuggingClaw-improved/start.sh`
- `HuggingClaw-improved/Dockerfile`

These files include the improvements made during this session (notably DEV_MODE-gated JupyterLab startup and safer Jupyter symlink/workspace handling).

## Upstream project reference
Target upstream repository:
- https://github.com/anurag162008/HuggingClaw

> Note: In this runtime, direct clone/push to GitHub was blocked by proxy (HTTP CONNECT 403), so this bundle is created locally for transfer/apply.
